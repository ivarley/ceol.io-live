{
  "name": "ceol-live-logging",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Spec 024 live-logging screen — clean-slate Svelte + Vite bundle, served by Flask under /static/live.",
  "scripts": {
    "dev": "vite build --watch",
    "dev:mytunes": "vite build --watch --config vite.mytunes.config.js",
    "build": "vite build && vite build --config vite.mytunes.config.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@testing-library/jest-dom": "^6.4.0",
    "@testing-library/svelte": "^5.2.0",
    "@vitest/coverage-v8": "^2.1.0",
    "fake-indexeddb": "^6.0.0",
    "jsdom": "^25.0.0",
    "svelte": "^5.0.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
